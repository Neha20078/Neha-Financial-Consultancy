
  # Responsive Website UI Design (Copy)

  This is a code bundle for Responsive Website UI Design (Copy). The original project is available at https://www.figma.com/design/korWvmmGMvtAPeZzl6L2kI/Responsive-Website-UI-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  